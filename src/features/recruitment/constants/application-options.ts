import { MAJORS } from "@/shared/constants/majors";
import { WORK_AREAS } from "@/shared/constants/work-areas";

export const MAJOR_VALUES = MAJORS.map((major) => major.value);

export const WORK_AREA_VALUES = WORK_AREAS.map((area) => area.value);
